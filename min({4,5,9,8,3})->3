min({a, b, c, d, e})
This is a C++11 feature that lets you find the minimum of multiple values at once using an initializer list {}.

It works like:
min({5, 2, 7, 3}) → 2





#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int t;
    cin >> t;  // number of test cases
    
    while (t--) {
        long long xC, xi, xa, xl, xo;
        cin >> xC >> xi >> xa >> xl >> xo;
        
        // Each "Ciallo" needs: C=1, i=1, a=1, l=2, o=1
        long long maxCiallo = min({xC / 1, xi / 1, xa / 1, xl / 2, xo / 1});
        
        cout << maxCiallo << endl;
    }
    
    return 0;
}
